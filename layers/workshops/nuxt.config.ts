// Layer « workshops » — module ateliers (page publique + gestion + API + lieux + import IA).
// Découplé du domaine côté serveur via les ports server/workshops/ports (flags/dates de l'Edition)
// et l'util core canManageWorkshopLocations (permissions de gestion des lieux).
export default defineNuxtConfig({})
