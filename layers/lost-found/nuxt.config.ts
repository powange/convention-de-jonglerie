// Layer « lost-found » — module objets trouvés (page publique + gestion + API). Découplé du domaine
// côté serveur via les ports server/lost-found/ports (existence/date d'événement en jonglerie).
export default defineNuxtConfig({})
