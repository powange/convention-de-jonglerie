// Layer « faq » — module FAQ modulaire (page publique + gestion + API). Découplé du domaine côté
// serveur via les ports server/faq/ports (visibilité propre à l'Edition en jonglerie).
export default defineNuxtConfig({})
