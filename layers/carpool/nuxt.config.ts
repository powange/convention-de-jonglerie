// Layer « carpool » — module covoiturage (offres, demandes, réservations, passagers, commentaires).
// Découplé du domaine côté serveur via le port server/carpool/ports (existence d'événement).
export default defineNuxtConfig({})
