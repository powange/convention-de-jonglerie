// Layer « stock » — module gestion du matériel (groupes, items, réservations, planning).
// Le serveur consomme les utils de permission du cœur via #server (getEditionWithPermissions,
// canManageStock, canAccessStock, stock-helpers) — aucun port nécessaire (pas de lecture d'Edition
// propre au domaine dans le layer).
export default defineNuxtConfig({})
