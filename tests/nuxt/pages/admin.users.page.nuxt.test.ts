import { describe, it, expect } from 'vitest'
// TODO: mocker /api/admin/users et vérifier filtres

describe('Page /admin/users', () => {
  it('smoke: devrait charger la page admin users', () => {
    expect(true).toBe(true)
  })
})
