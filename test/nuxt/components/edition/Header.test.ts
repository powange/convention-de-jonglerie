import { describe, it, expect } from 'vitest'
import EditionHeader from '../../../../app/components/edition/Header.vue'

describe('EditionHeader', () => {
  it('vérifie que le composant peut être importé', () => {
    expect(EditionHeader).toBeDefined()
  })
})
