import { describe, it, expect } from 'vitest'
import EditionPost from '../../../../app/components/edition/Post.vue'

describe('EditionPost', () => {
  it('vérifie que le composant peut être importé', () => {
    expect(EditionPost).toBeDefined()
  })
})
