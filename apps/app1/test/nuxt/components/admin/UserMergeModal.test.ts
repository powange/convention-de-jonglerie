import { describe, it, expect } from 'vitest'

import UserMergeModal from '../../../../app/components/admin/UserMergeModal.vue'

describe('UserMergeModal', () => {
  it('vérifie que le composant peut être importé', () => {
    expect(UserMergeModal).toBeDefined()
  })
})
