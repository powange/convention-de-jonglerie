import { describe, it, expect } from 'vitest'

import ImportFromImageModal from '../../../../../../layers/workshops/app/components/workshops/ImportFromImageModal.vue'

describe('workshops ImportFromImageModal', () => {
  it('vérifie que le composant peut être importé', () => {
    expect(ImportFromImageModal).toBeDefined()
  })
})
