# Configuration Docker pour Convention de Jonglerie
# Copiez ce fichier vers .env.docker et modifiez les valeurs

# Base de données MySQL
DATABASE_URL="mysql://convention_user:convention_password@database:3306/convention_db"

# JWT Secret - CHANGEZ CETTE VALEUR EN PRODUCTION
JWT_SECRET="your-super-secret-jwt-key-change-in-production-make-it-very-long-and-random"

# Configuration MySQL
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=convention_db
MYSQL_USER=convention_user
MYSQL_PASSWORD=convention_password

# Configuration Nuxt
NUXT_HOST=0.0.0.0
NUXT_PORT=3000
NODE_ENV=production

# URL de l'application (pour les liens absolus)
APP_URL=http://localhost:3000